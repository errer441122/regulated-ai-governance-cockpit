# Copy this file to .env before running docker compose locally.
# Keep .env out of Git and replace these placeholders with local-only values.
MINIO_ROOT_USER=replace-with-local-minio-user
MINIO_ROOT_PASSWORD=replace-with-local-minio-password
